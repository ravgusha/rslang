export const Game = {
  mode: 'main', // available modes for example : main, e-book, sprint, audiocall, stat
  authorized: false, // if false -> render 6 chapters in e-book / else 7 chapters
};
const BASE_URL = 'https://rslangteam.herokuapp.com';

export default BASE_URL;
